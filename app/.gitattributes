# Auto detect text files and perform LF normalization
* text=auto

# Force LF for shell scripts
*.sh text eol=lf

# Force CRLF for Windows batch files
*.bat text eol=crlf

# Netlify configuration
netlify.toml text eol=lf
netlify/functions/*.js text eol=lf
